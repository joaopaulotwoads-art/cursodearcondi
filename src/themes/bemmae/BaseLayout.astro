---
import Header from './Header.astro';
import Footer from './Footer.astro';
import type { NavItem } from './types';
/** Só subset latin (pt-BR) — evita carregar latin-ext em paralelo (menos woff2 no caminho crítico). */
import '@fontsource/outfit/latin-400.css';
import '@fontsource/outfit/latin-500.css';
import '@fontsource/outfit/latin-600.css';
import '@fontsource/outfit/latin-700.css';
import './global.css';
import { buildCanonicalPageUrl, ensureApexSiteOrigin } from '../../utils/read-site-settings';

interface Props {
  title: string;
  description?: string;
  siteName: string;
  /** Caminho público do logo (ex.: /bemmae-logo.png) quando definido no singleton Menu. */
  logoSrc?: string;
  siteUrl: string;
  nav: NavItem[];
  footerNav: NavItem[];
  /** Singletons do rodapé (ex.: bemmae) */
  contentThemeId?: string;
  ogType?: 'website' | 'article';
  ogImageAbs?: string;
  articlePublishedTime?: string;
  articleModifiedTime?: string;
  extraJsonLd?: Record<string, unknown> | null;
}

const {
  title,
  description,
  siteName,
  logoSrc,
  siteUrl,
  nav,
  footerNav,
  contentThemeId = 'bemmae',
  ogType = 'website',
  ogImageAbs,
  articlePublishedTime,
  articleModifiedTime,
  extraJsonLd,
} = Astro.props;
/** Título do documento: o que cada página passa em `title` (sem sufixo automático | marca). */
const fullTitle = title;
const fullDescription = description || '';
const siteOrigin = ensureApexSiteOrigin(siteUrl);
const canonicalPageUrl = buildCanonicalPageUrl(siteUrl, Astro.url.pathname);
---
<!DOCTYPE html>
<html lang="pt-BR" class="bemmae-root">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" type="image/svg+xml" href="/bemmae-favicon.svg" sizes="any" />
  <link rel="alternate icon" href="/bemmae-favicon.svg" />
  <link rel="apple-touch-icon" href="/bemmae-favicon.svg" />
  <meta name="theme-color" content="#faf8f6" />
  <title>{fullTitle}</title>
  <meta name="description" content={fullDescription} />
  <link rel="canonical" href={canonicalPageUrl} />
  <meta property="og:type" content={ogType} />
  <meta property="og:title" content={fullTitle} />
  <meta property="og:description" content={fullDescription} />
  <meta property="og:url" content={canonicalPageUrl} />
  <meta property="og:locale" content="pt_BR" />
  {ogImageAbs && <meta property="og:image" content={ogImageAbs} />}
  {ogType === 'article' && articlePublishedTime && (
    <meta property="article:published_time" content={articlePublishedTime} />
  )}
  {ogType === 'article' && articleModifiedTime && (
    <meta property="article:modified_time" content={articleModifiedTime} />
  )}
  <meta name="twitter:card" content={ogImageAbs ? 'summary_large_image' : 'summary'} />
  <meta name="twitter:title" content={fullTitle} />
  <meta name="twitter:description" content={fullDescription} />
  {ogImageAbs && <meta name="twitter:image" content={ogImageAbs} />}
  <script type="application/ld+json" set:html={JSON.stringify({ '@context': 'https://schema.org', '@type': 'WebSite', name: siteName, url: siteOrigin || siteUrl, description: fullDescription || undefined })} />
  {extraJsonLd && (
    <script type="application/ld+json" set:html={JSON.stringify(extraJsonLd)} />
  )}
</head>
<body class="bemmae-scope bg-[var(--color-bg)] text-[var(--color-text)] antialiased">
  <Header siteName={siteName} logoSrc={logoSrc} nav={nav} />
  <main><slot /></main>
  <Footer siteName={siteName} logoSrc={logoSrc} footerNav={footerNav} contentThemeId={contentThemeId} />
</body>
</html>
